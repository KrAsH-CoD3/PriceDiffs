[project]
name = "pricediff"
version = "0.2.0"
description = "Price tracking dashboard"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cloakbrowser>=0.3.31",
    "django>=6.0.5",
    "httpx>=0.28.1",
    "playwright>=1.60.0",
]

[tool.django]
settings = "pricediff.settings"
